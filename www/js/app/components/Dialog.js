/**
 * @module Application
 */
define([
    "framework/GelatoDialog"
], function(GelatoDialog) {
    /**
     * @class Dialog
     * @extends GelatoDialog
     */
    return GelatoDialog.extend({});
});
