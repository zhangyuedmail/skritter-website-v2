define([
    "framework/GelatoModel"
], function(GelatoModel) {
    return GelatoModel.extend({
        /**
         * @class UserSettings
         * @extends GelatoModel
         * @constructor
         */
        initialize: function() {}
    });
});
