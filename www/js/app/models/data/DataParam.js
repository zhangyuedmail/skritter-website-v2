/**
 * @module Application
 */
define([
   "framework/GelatoModel"
], function(GelatoModel) {
    /**
     * @class DataParam
     * @extends GelatoModel
     */
    return GelatoModel.extend({
    });
});
