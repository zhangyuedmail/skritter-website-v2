/**
 * @module Application
 */
define([
   'framework/BaseModel'
], function(BaseModel) {
    /**
     * @class DataParam
     * @extends BaseModel
     */
    var DataParam = BaseModel.extend({});

    return DataParam;
});
