define([], function() {
    /**
     * @class Schema
     */
    var Model = Backbone.Model.extend({
        /**
         * @property {Object} defaults
         */
        defaults: {
        }
    });

    return Model;
});