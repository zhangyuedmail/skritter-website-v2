gelato-page(data-name='mobile-home')

  .welcome-panel
    .title
      .logo
    .language.no-select= app.getLanguageName()
    .character.no-select(class=app.getLanguage() === 'zh' ? 'text-chinese' : 'text-japanese') 字

    .welcome-options
      .row
        .col-xs-6
          a.btn.btn-fixed.btn-success(href='/signup')
            div= app.locale('common.getStarted')
            div.helper-text (new users)
        .col-xs-6.text-center
          a.btn.btn-fixed.btn-success(href='/login')
            div= app.locale('common.logIn')
            div.helper-text (existing users)
