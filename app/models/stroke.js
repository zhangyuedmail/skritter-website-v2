var GelatoModel = require('gelato/model');

/**
 * @class Stroke
 * @extends {GelatoModel}
 */
module.exports = GelatoModel.extend({
    /**
     * @property idAttribute
     * @type {String}
     */
    idAttribute: 'rune'
});
