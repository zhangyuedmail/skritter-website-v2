var Model = require('base/model');

/**
 * @class Decomp
 * @extends {Model}
 */
module.exports = Model.extend({
    /**
     * @property idAttribute
     * @type String
     */
    idAttribute: 'writing'
});
