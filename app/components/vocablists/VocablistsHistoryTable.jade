gelato-component(data-name='vocablist-history-table')

  if view.history.state === 'fetching'
    p.text-center
      i.fa.fa-3x.fa-spinner.fa-pulse

  else if view.history.length
      for history, i in view.history.models
        .row(class=i%2===0 ? 'even' : 'odd')
          .col-md-4= history.get('displayName')
          .col-md-6= history.getChanges()
          .col-md-2= history.getChangedDate()
  else
    .row= app.locale('dialogs.vocablistHistory.noHistory')
