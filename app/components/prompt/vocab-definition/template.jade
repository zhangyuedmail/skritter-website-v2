gelato-component(data-name='prompt-vocab-definition')
  - var prompt = view.prompt
  - var reviews = view.prompt.reviews

  if reviews
    - definiton = reviews.vocab.getDefinition();
    label Definition

    div.value
      if definiton
        = definiton
      else
        | Click here to add your own custom definition
